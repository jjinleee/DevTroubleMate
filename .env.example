# Copy this file to .env for Docker Compose only.
# Do not commit the copied .env file.
DB_NAME=devtroublemate
DB_USERNAME=postgres
DB_PASSWORD=replace-with-local-password
DB_PORT=5432
TEST_DB_NAME=devtroublemate_test

# Configure this in the Spring Boot/IntelliJ environment, not Docker Compose.
OPENAI_API_KEY=replace-with-openai-api-key
